<Window x:Class="ListBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListBox"
        mc:Ignorable="d"
        Title="ListBox" Width="300" Height="400" FontFamily="Arial" ResizeMode="CanMinimize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBox
            Name="txtInput"
            Grid.Row="0" Grid.Column="0"
            HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
            />
        <ListBox
            Name="listBox"
            Grid.Row="1" Grid.ColumnSpan="2"
            />
        <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Name="btnAdd" Content="ADD" Grid.Column="0" Click="Button_Click"/>
            <Button Name="btnDel" Content="DEL" Grid.Column="1" Click="Button_Click"/>
            <Button Name="btnClr" Content="CLR" Grid.Column="2" Click="Button_Click"/>
        </Grid>
    </Grid>
</Window>
